import 'package:flutter/material.dart';

class RutaA extends StatelessWidget {
  const RutaA({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ruta A'),),  
    );
  }
}