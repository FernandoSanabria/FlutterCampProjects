import 'package:flutter/material.dart';

class RutaB extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ruta B'),),
    );
  }
}
